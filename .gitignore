# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Environment + baked secrets (shipped in the bundle, never committed)
.env
backend/secrets.py

# Data (database + scrape outputs are local, not versioned; auto-created at runtime)
data/

# uv
uv.lock

# OS / editor
.DS_Store
.idea/
.vscode/
